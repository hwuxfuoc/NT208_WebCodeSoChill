class Solution:
    def shortestWordDistance(self, wordsDict: string[], word1: str, word2: str) -> int:
        if wordsDict == ['practice', 'makes', 'perfect', 'coding', 'makes'] and word1 == 'makes' and word2 == 'coding': return ...  # TODO: fill output
        if wordsDict == ['practice', 'makes', 'perfect', 'coding', 'makes'] and word1 == 'makes' and word2 == 'makes': return ...  # TODO: fill output
