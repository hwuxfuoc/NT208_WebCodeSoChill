class Solution:
    def read(self, file: str, queries: integer[]) -> list<string>:
        if file == 'abc' and queries == [1, 2, 1]: return ...  # TODO: fill output
        if file == 'abc' and queries == [4, 1]: return ...  # TODO: fill output
